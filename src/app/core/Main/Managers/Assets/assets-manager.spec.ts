import { AssetsManager } from './assets-manager';
import {DivineStar} from "../../DivineStar";
describe('AssetsManager', () => {
  it('should create an instance', () => {
    //expect(new AssetsManager()).toBeTruthy();
  });
});
