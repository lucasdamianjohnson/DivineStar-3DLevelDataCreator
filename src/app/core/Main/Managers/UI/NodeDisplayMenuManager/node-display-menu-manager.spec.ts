import { NodeDisplayMenuManager } from './node-display-menu-manager';

describe('NodeDisplayMenuManager', () => {
  it('should create an instance', () => {
    expect(new NodeDisplayMenuManager()).toBeTruthy();
  });
});
