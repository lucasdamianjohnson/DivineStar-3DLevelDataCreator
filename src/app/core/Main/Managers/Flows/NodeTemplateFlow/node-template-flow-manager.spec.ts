import { NodeTemplateFlowManager } from './node-template-flow-manager';

describe('NodeTemplateFlowManager', () => {
  it('should create an instance', () => {
    expect(new NodeTemplateFlowManager()).toBeTruthy();
  });
});
