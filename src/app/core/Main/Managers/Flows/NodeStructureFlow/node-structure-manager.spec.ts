import { NodeStructureManager } from './node-structure-manager';

describe('NodeStructureManager', () => {
  it('should create an instance', () => {
    expect(new NodeStructureManager()).toBeTruthy();
  });
});
