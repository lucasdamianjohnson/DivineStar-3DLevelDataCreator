import { NodeTemplatesMenuManager } from './node-templates-menu-manager';

describe('NodeTemplatesMenuManager', () => {
  it('should create an instance', () => {
    expect(new NodeTemplatesMenuManager()).toBeTruthy();
  });
});
