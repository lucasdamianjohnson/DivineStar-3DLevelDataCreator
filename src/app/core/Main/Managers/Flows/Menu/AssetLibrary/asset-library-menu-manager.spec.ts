import { AssetLibraryMenuManager } from './asset-library-menu-manager';

describe('AssetLibraryMenuManager', () => {
  it('should create an instance', () => {
    expect(new AssetLibraryMenuManager()).toBeTruthy();
  });
});
