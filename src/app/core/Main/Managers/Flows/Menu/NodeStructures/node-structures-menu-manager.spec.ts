import { NodeStructuresMenuManager } from './node-structures-menu-manager';

describe('NodeStructuresMenuManager', () => {
  it('should create an instance', () => {
    expect(new NodeStructuresMenuManager()).toBeTruthy();
  });
});
